// SPDX-License-Identifier: GPL-3.0-or-later

fn main() {
    eprintln!(
        "backend comparison harness placeholder; run with --features dx12,benchmark once the renderer is wired to real frames"
    );
}
